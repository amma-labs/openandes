---
// Este componente obtiene el último post de Fun Politik y 2050lab desde sus feeds RSS

const parseRSS = (xml) => {
  const items = [];
  const itemRegex = /<item>([\s\S]*?)<\/item>/g;
  let match;
  
  while ((match = itemRegex.exec(xml)) !== null) {
    const itemXml = match[1];
    let title = itemXml.match(/<title>([\s\S]*?)<\/title>/)?.[1] || 'Sin título';
    title = title.replace(/<!\[CDATA\[/g, '').replace(/\]\]>/g, '').trim();
    
    let description = itemXml.match(/<description>([\s\S]*?)<\/description>/)?.[1] || '';
    description = description.replace(/<!\[CDATA\[/g, '').replace(/\]\]>/g, '').trim();
    
    const link = itemXml.match(/<link>([\s\S]*?)<\/link>/)?.[1] || '';
    const pubDate = itemXml.match(/<pubDate>([\s\S]*?)<\/pubDate>/)?.[1] || '';
    const enclosure = itemXml.match(/<enclosure url="([^"]+)"/)?.[1] || '';
    const content = itemXml.match(/<content:encoded><\!\[CDATA\[([\s\S]*?)\]\]><\/content:encoded>/)?.[1] || '';
    
    let imageUrl = enclosure;
    if (!imageUrl) {
      const imgMatch = content.match(/<img[^>]+src="([^">]+)"/);
      if (imgMatch) imageUrl = imgMatch[1];
    }
    
    items.push({
      title: title,
      link: link.trim(),
      pubDate: pubDate.trim(),
      description: description,
      image: imageUrl,
      content: content
    });
  }
  return items;
};

let funPolitikPosts = [];
let futuresLabPosts = [];

const fetchFeed = async (url) => {
  try {
    const response = await fetch(url, {
      headers: { 'User-Agent': 'Mozilla/5.0' }
    });
    if (response.ok) {
      return await response.text();
    }
    return null;
  } catch (error) {
    console.error(`Error loading feed ${url}:`, error);
    return null;
  }
};

const funPolitikXml = await fetchFeed('https://funpolitik.substack.com/feed');
if (funPolitikXml) {
  funPolitikPosts = parseRSS(funPolitikXml);
}

const futuresLabXml = await fetchFeed('https://www.2050lab.org/feed');
if (futuresLabXml) {
  futuresLabPosts = parseRSS(futuresLabXml);
}

const getLastPost = (posts) => {
  if (!posts || posts.length === 0) return null;
  return posts.sort((a, b) => 
    new Date(b.pubDate).getTime() - new Date(a.pubDate).getTime()
  )[0];
};

const lastFunPolitik = getLastPost(funPolitikPosts);
const lastFuturesLab = getLastPost(futuresLabPosts);
---

<div class="grid grid-cols-1 md:grid-cols-2 gap-6 mt-8">
  <!-- Fun Politik -->
  <div class="p-6 bg-[#f8f4ea]/50 dark:bg-[#1a1a1a]/50 rounded-lg border border-[#e8e0d0] dark:border-[#2a2a2a]">
    <div class="flex items-center gap-2 mb-3">
      <img 
        src="https://substackcdn.com/image/fetch/$s_!IeqO!,w_48,c_limit,f_auto,q_auto:good,fl_progressive:steep/https%3A%2F%2Ffunpolitik.substack.com%2Fimg%2Fsubstack.png" 
        alt="Fun Politik"
        class="w-6 h-6 rounded"
      />
      <h3 class="text-xl font-bold">Fun Politik</h3>
      
    </div>
    
    {lastFunPolitik ? (
      <div class="border-t border-[#e8e0d0] dark:border-[#2a2a2a] pt-3">
        {lastFunPolitik.image && (
          <div class="overflow-hidden rounded mb-3">
            <img 
              src={lastFunPolitik.image} 
              alt={lastFunPolitik.title}
              class="w-full h-48 object-cover"
              loading="lazy"
            />
          </div>
        )}
        <p class="text-xs text-muted mb-1">
          {new Date(lastFunPolitik.pubDate).toLocaleDateString('es-ES', { 
            day: 'numeric', month: 'long', year: 'numeric' 
          })}
        </p>
        <a 
          href={lastFunPolitik.link}
          target="_blank" 
          rel="noopener"
          class="font-medium text-[#1a4b8c] dark:text-[#2b6cb5] hover:underline text-sm"
        >
          {lastFunPolitik.title}
        </a>
        <p class="text-xs text-muted mt-1 line-clamp-3">
          {lastFunPolitik.description}
        </p>
        <div class="mt-3 pt-3 border-t border-[#e8e0d0] dark:border-[#2a2a2a]">
          <a 
            href={lastFunPolitik.link}
            target="_blank" 
            rel="noopener"
            class="text-xs text-[#1a4b8c] dark:text-[#2b6cb5] hover:underline font-medium"
          >
            Leer artículo completo →
          </a>
        </div>
      </div>
    ) : (
      <div class="border-t border-[#e8e0d0] dark:border-[#2a2a2a] pt-3">
        <p class="text-sm text-muted">No se pudo cargar el feed</p>
        <a 
          href="https://funpolitik.substack.com"
          target="_blank" 
          rel="noopener"
          class="text-xs text-[#1a4b8c] dark:text-[#2b6cb5] hover:underline"
        >
          Ver en Substack →
        </a>
      </div>
    )}
    
    <div class="mt-4 pt-3 border-t border-[#e8e0d0] dark:border-[#2a2a2a]">
      <a 
        href="https://funpolitik.substack.com"
        target="_blank" 
        rel="noopener"
        class="text-xs text-[#1a4b8c] dark:text-[#2b6cb5] hover:underline"
      >
        Ver todas las publicaciones →
      </a>
    </div>
  </div>

  <!-- 2050 Futures Lab -->
  <div class="p-6 bg-[#f8f4ea]/50 dark:bg-[#1a1a1a]/50 rounded-lg border border-[#e8e0d0] dark:border-[#2a2a2a]">
    <div class="flex items-center gap-2 mb-3">
      <img 
        src="https://substackcdn.com/image/fetch/$s_!qnb5!,w_48,c_limit,f_auto,q_auto:good,fl_progressive:steep/https%3A%2F%2Fsubstack-post-media.s3.amazonaws.com%2Fpublic%2Fimages%2F7d01f6a5-7f87-497b-aa94-bd8d7a6b956f_270x270.png" 
        alt="2050 Futures Lab"
        class="w-6 h-6 rounded"
      />
      <h3 class="text-xl font-bold">2050 Futures Lab</h3>
      
    </div>
    
    {lastFuturesLab ? (
      <div class="border-t border-[#e8e0d0] dark:border-[#2a2a2a] pt-3">
        {lastFuturesLab.image && (
          <div class="overflow-hidden rounded mb-3">
            <img 
              src={lastFuturesLab.image} 
              alt={lastFuturesLab.title}
              class="w-full h-48 object-cover"
              loading="lazy"
            />
          </div>
        )}
        <p class="text-xs text-muted mb-1">
          {new Date(lastFuturesLab.pubDate).toLocaleDateString('es-ES', { 
            day: 'numeric', month: 'long', year: 'numeric' 
          })}
        </p>
        <a 
          href={lastFuturesLab.link}
          target="_blank" 
          rel="noopener"
          class="font-medium text-[#1a4b8c] dark:text-[#2b6cb5] hover:underline text-sm"
        >
          {lastFuturesLab.title}
        </a>
        <p class="text-xs text-muted mt-1 line-clamp-3">
          {lastFuturesLab.description}
        </p>
        <div class="mt-3 pt-3 border-t border-[#e8e0d0] dark:border-[#2a2a2a]">
          <a 
            href={lastFuturesLab.link}
            target="_blank" 
            rel="noopener"
            class="text-xs text-[#1a4b8c] dark:text-[#2b6cb5] hover:underline font-medium"
          >
            Leer artículo completo →
          </a>
        </div>
      </div>
    ) : (
      <div class="border-t border-[#e8e0d0] dark:border-[#2a2a2a] pt-3">
        <p class="text-sm text-muted">No se pudo cargar el feed</p>
        <a 
          href="https://2050lab.substack.com"
          target="_blank" 
          rel="noopener"
          class="text-xs text-[#1a4b8c] dark:text-[#2b6cb5] hover:underline"
        >
          Ver en Substack →
        </a>
      </div>
    )}
    
    <div class="mt-4 pt-3 border-t border-[#e8e0d0] dark:border-[#2a2a2a]">
      <a 
        href="https://2050lab.substack.com"
        target="_blank" 
        rel="noopener"
        class="text-xs text-[#1a4b8c] dark:text-[#2b6cb5] hover:underline"
      >
        Ver todas las publicaciones →
      </a>
    </div>
  </div>
</div>
